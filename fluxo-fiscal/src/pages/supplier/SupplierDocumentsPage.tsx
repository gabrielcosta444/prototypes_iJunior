import { ChevronDown, Search } from 'lucide-react';
import { PageHeader } from '../../components/ui/PageHeader';
import { StatusBadge } from '../../components/ui/StatusBadge';
import { documents } from '../../data/mockData';
export function SupplierDocumentsPage() { const own = documents.filter((item) => item.supplier === 'Morganite Brasil' || item.id === 'nfse-55219'); return <div><PageHeader title="Meus Documentos" subtitle="Todos os documentos enviados pela Morganite Brasil para a Allebras." /><section className="secondary-toolbar card"><label><Search size={15} /><input placeholder="Buscar documento..." /></label><button>Status<ChevronDown size={13} /></button><button>Período<ChevronDown size={13} /></button></section><section className="data-card"><div className="table-scroll"><table className="data-table"><thead><tr><th>Documento</th><th>Tipo</th><th>Enviado em</th><th>Valor</th><th>Pedido</th><th>Status</th></tr></thead><tbody>{own.map((item, index) => <tr key={item.id}><td><strong>{item.title}</strong></td><td>{item.type}</td><td>{index ? '18/08/2026, 11:06' : 'Hoje, 10:24'}</td><td><strong>{item.value}</strong></td><td>PC-2026-00494{index + 5}</td><td><StatusBadge status={item.status} /></td></tr>)}</tbody></table></div><footer className="table-footer"><span>42 documentos enviados</span><span>Página 1 de 7</span></footer></section></div>; }
