import { AlertTriangle, ArrowRight, FileText } from 'lucide-react';
import { Button } from '../../components/ui/Button';
import { PageHeader } from '../../components/ui/PageHeader';
const ownPendencies = [['NF-e 000023781', 'Categoria não confirmada', 'Confirme o pedido e aguarde a análise da Allebras.'], ['Declaração 08-2026', 'Assinatura não identificada', 'Envie uma versão assinada do documento.'], ['NF-e 000023748', 'Pedido de compra divergente', 'Revise o número do pedido informado.']];
export function SupplierPendenciesPage() { return <div><PageHeader title="Pendências" subtitle="Documentos que precisam de informações adicionais da sua empresa." /><section className="supplier-pendency-summary"><AlertTriangle size={20} /><div><strong>3 documentos precisam da sua atenção</strong><p>Complete os itens abaixo para que a Allebras conclua o processamento.</p></div></section><section className="supplier-pendency-list">{ownPendencies.map(([document, reason, help]) => <article className="card" key={document}><span><FileText size={18} /></span><div><strong>{document}</strong><p>{reason}</p><small>{help}</small></div><Button variant="secondary" size="sm">Resolver <ArrowRight size={13} /></Button></article>)}</section></div>; }
