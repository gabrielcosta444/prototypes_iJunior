import { ArrowLeft, Camera, CheckCircle2, GripVertical, MapPin, Plus, Save, Trash2 } from 'lucide-react';
import { Link } from 'react-router-dom';
import { PageHeader } from '../../components/ui/PageHeader';
import { checklistItems } from '../../data/mockData';

export function ChecklistEditorPage() { return <div><PageHeader title="Checklist — Roçagem" subtitle="Modelo operacional v3 · Publicado" actions={<><Link className="button button--secondary" to="/admin/checklists"><ArrowLeft size={17} /> Modelos</Link><button className="button button--primary"><Save size={17} /> Salvar alterações</button></>} /><div className="editor-layout"><section className="panel checklist-editor"><div className="form-grid"><label className="span-2">Nome do modelo<input defaultValue="Checklist — Roçagem" /></label><label>Tipo de serviço<select><option>Roçagem</option></select></label><label>Versão<input defaultValue="v3" /></label></div><header><span>ITENS DO CHECKLIST</span><button><Plus size={16} /> Adicionar item</button></header>{checklistItems.map((item, i) => <article className="editor-item" key={item.id}><GripVertical /><span className="editor-number">{i + 1}</span><div><strong>{item.label}</strong><span><b>Obrigatório</b>{item.photo && <b><Camera size={12} /> Foto necessária</b>}{item.location && <b><MapPin size={12} /> Localização necessária</b>}</span></div><button><Trash2 size={16} /></button></article>)}</section><aside className="panel editor-preview"><span className="eyebrow">PRÉVIA MOBILE</span><h2>Checklist</h2><p>0 de 6 itens concluídos</p><div className="progress-bar"><i style={{ width: '0%' }} /></div>{checklistItems.map((item) => <div key={item.id}><span /><strong>{item.label}</strong>{item.photo && <Camera size={14} />}</div>)}<footer><CheckCircle2 size={17} /> Todos os itens são obrigatórios</footer></aside></div></div>; }
