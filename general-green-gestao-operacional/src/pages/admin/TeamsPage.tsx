import { MapPin, Plus, Radio, UsersRound } from 'lucide-react';
import { PageHeader } from '../../components/ui/PageHeader';
import { teams } from '../../data/mockData';

export function TeamsPage() { return <div><PageHeader title="Equipes de Campo" subtitle="Disponibilidade, mobilização e produtividade das equipes." actions={<button className="button button--primary"><Plus size={17} /> Nova equipe</button>} /><section className="team-cards">{teams.map((team) => <article className="team-card panel" key={team.id}><header><span className="team-icon"><UsersRound size={20} /></span><span className={`team-state team-state--${team.status}`}><i />{team.status === 'em-operacao' ? 'Em operação' : 'Disponível'}</span></header><h2>{team.name}</h2><p>{team.leader} · Líder de equipe</p><div className="team-members">{Array.from({ length: Math.min(5, team.members) }).map((_, i) => <span key={i}>{['CO','JR','AM','RS','FP'][i]}</span>)}<small>+{Math.max(0, team.members - 5)}</small></div>{team.currentOrder ? <div className="team-assignment"><Radio size={17} /><span><small>ATIVIDADE ATUAL</small><strong>{team.currentOrder} · {team.plant}</strong></span></div> : <div className="team-assignment available"><MapPin size={17} /><span><small>DISPONIBILIDADE</small><strong>Pronta para programação</strong></span></div>}<footer><span><small>Produção no mês</small><strong>{team.id === 'norte' ? '62,4 ha' : team.id === 'sul' ? '54,8 ha' : '38.400 módulos'}</strong></span><button className="button button--secondary">Ver equipe</button></footer></article>)}</section></div>; }
