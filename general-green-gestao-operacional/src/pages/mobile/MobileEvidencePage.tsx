import { ArrowLeft, Camera, CheckCircle2, Clock3, LocateFixed, MapPin, Plus } from 'lucide-react';
import { Link } from 'react-router-dom';
import { usePrototype } from '../../app/PrototypeContext';

export function MobileEvidencePage() { const { queueEvidence, queuedPhotos } = usePrototype(); return <div className="mobile-page"><Link className="mobile-back" to="/mobile/activity/2026-0148"><ArrowLeft /> Atividade</Link><h1>Evidências</h1><p className="mobile-subtitle">Registre o antes, durante e depois da execução.</p><div className="evidence-status"><CheckCircle2 /><span><strong>Localização ativa</strong><small>Precisão atual: 4 metros</small></span></div><section className="mobile-evidence-list">{[['Antes','08:02','complete'],['Durante','11:43','complete'],['Depois','Pendente','pending']].map(([stage,time,status], index) => <article key={stage}><div className={`mobile-photo mobile-photo--${index}`}>{status === 'pending' ? <Camera /> : <span><CheckCircle2 /> Registrada</span>}</div><div><strong>{stage}</strong><small><Clock3 /> {time}</small>{status === 'complete' && <small><MapPin /> -19.9123, -44.0824</small>}</div>{status === 'pending' && <button onClick={queueEvidence}><Plus /> Adicionar</button>}</article>)}</section><div className="mobile-location-card"><LocateFixed /><span><small>POSIÇÃO DO ÚLTIMO REGISTRO</small><strong>-19.912318, -44.082401</strong><p>UFV Sol do Cerrado · Bloco B07</p></span></div><small className="mobile-queue-label">{queuedPhotos} fotos salvas no dispositivo</small><Link className="mobile-primary-button" to="/mobile/activity/2026-0148">Salvar e voltar</Link></div>; }
